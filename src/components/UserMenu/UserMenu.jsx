import { Box } from '@mui/material';

export const UserMenu = () => {
  return (
    <>
      <Box>
        <p>Welcome, User</p>
        <button type="button" onClick={() => {}}>
          Logout
        </button>
      </Box>
    </>
  );
};
